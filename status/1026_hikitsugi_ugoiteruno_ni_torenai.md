# 引き継ぎ 1026 ―「動いているのに何も取れていない」を全部洗って潰した（2026-09-23 未明）

たまごさんは寝ています。次のセッションはここから続けてください。

---

## 0. 先に結論（3行）

- 実測で赤は **21件**。うち **13件を潰した**（見えていなかった8件＋数の嘘1件＋説明の嘘3件＋969番）。
- 残った赤 **8件**は、全部 **たまごさんにしか外せない** か **課金・権利がいる**。理由つきで表に出してある。隠していない。
- 次の一手：**Claudeのアプリで1回ログインし直す**（これ1つで工場が動き出す）と、
  **`/Users/mac/.tamago/gmail_app_password` を置く**（これ1つで見張り4本が同時に開く）。

---

## 1. 実測のやり方（誰でも同じ数字が出せる）

```bash
cd ~/Desktop/tamago-shinchoku
python3 tools/hantei.py        # ← 赤の正本。これ以外に判定を書かない
```

`tools/hantei.py` が唯一の判定。`kagi_daicho.py` に残っていた**同じ規則の写し**は今回消した
（1018番で引っ越したはずが、古い写しが残っていた＝片方だけ直る状態だった）。

---

## 2. 潰した赤（12件）

### ★① 台帳外の定期便 8本 ― 走っているのに誰も見ていなかった

`kagi_daicho.unlisted_scan()` は前から見つけていた。**なのに、その結果は台帳JSONの奥にしか出ず、
赤の一覧には1行も出ていなかった。**見つけているのに出していない＝黙って飲み込んでいたのと同じ。

出ていなかった8本：
`_1024_nyuka_hiduke` / `ai_daicho` / `commit_kuchi` / `gaibu_copy_naoshi` /
`gaibu_copy_nippou` / `mac_souji` / `oni_kuchi` / `shuhen_horu`

やったこと（**穴を塞がず、素材を替えた**）：

1. 8本とも台帳に載せて、それぞれ「何をもって**取れた**とするか」を決めた
   （例：コミットの口＝ログに `✅ 回収` があるか／周辺を掘る＝`candidates` の数）。
   `ai_daicho` だけは `hantei.gaibu_ai()` が既に投げ／返りで測っているので二重に数えない。
2. ★**`kagi_daicho.auto_watchers()` を足した。**心臓と5分便の台本から実際に呼ばれているものを読んで、
   名簿に無いものを**自動で台帳に入れる**。跡が無いものは「跡を残していないので測れません」と赤で出す。
   → **手書きの名簿だから毎回増えていた**（09-20に2本、09-23に8本）。もう名簿に足し忘れても隠れない。

### ★② 「78回走って0件」という数字そのものが嘘だった

LINE審査結果の見張りは78回走ったことになっていたが、**1度もGmailに繋ぎに行っていない。**
鍵が無いので `"blocked": "no_credential"` と紙に書いて即returnしていただけ。
それを「走った」と数えていたので、直しようのない赤が毎分ふくらんでいた。

- `hantei.tally()` を作って、**数え方も1か所に**した。**叩けもしなかった回は走ったに数えない。**
- `check_anthropic_reply.py` / `check_line_reply.py` / `check_line_shinsa.py` の `log_state()` を直した。
  **理由が前と同じなら紙を書き換えない**（＝催促も回数も増やさない。たまごさんの指示そのまま）。
  逆に**鍵が戻って実際に読めた回は、前の止め札を必ず消す**（消さないと直ったのに赤が残る＝新しい嘘）。
- 実測テスト合格：鍵が無い10回→走った0回／鍵が戻ったあと3回→走った3・取れた3／同じ跡を二度数えない。

### ★③ 説明の嘘 3件（Grok・Genspark・Copilot）

台帳には `blocked=1` と**理由まで書いてあった**のに、過去に投げた跡があるせいで
「**走った3回 → 取れた0回。動いているのに何も産んでいません**」とだけ出ていた。
＝壊れているように見えて、本当は「**課金・権利が無くて口が閉まっている**」。
`hantei.gaibu_ai()` が理由を `judge()` に渡していなかっただけ。渡すようにした。

あわせて `hantei.yakusu()`（機械語→読める日本語）を足した。`blocked=no_credential` のような
そのままでは読めない短い機械語だけを訳す。**日本語で理由が書いてあるものには触らない**
（最初の版は「403」の3文字を見ただけで Genspark の説明を丸ごとすり替えていた。それも新しい嘘なので直した）。

### ★④ 進捗表の一番上に「走った→取れた」が出ていなかった

1018番でこの形にしたはずだった。**実測したら、出ていたのは `status/genzaichi.md`
（Dispatchが読む紙）だけで、たまごさんが実際に開く `index.html` には1文字も無かった。**
届く場所に届いていない＝これも「動いているのに何も取れていない」。

- `kagi_daicho.build()` が `kagi_daicho_top.json` に `kasegi`（走った／取れた／空回し／止めている理由）を書く。
  数字は `hantei.kojo()` から取る（ここで数え直さない）。
- `index.html` の一番上（鍵の赤より上）に出す。**読めなかったときは緑にせず、赤で「読めません」と出す。**
- JSの構文チェック済み（`node --check`）。

---

## 3. ★969番（アバターの返信拾い）― 直した。ただし「返事が来た」はまだ

原因は既に確定していた（`status/969_返信0件の原因.md`）：**誰も起こしていなかった。**
977番でも同じ事故＝**2回目**＝個人の落ち度ではなく仕組みの不在。

だから「今回の的に引き金を1回打つ」では直らない（それは穴を塞ぐだけ）。
**見張りと引き金を同じ1本にした。**

`tools/_969_avatar_reply_pickup.py`：

- `targets.json` に **`wake`（起こし方）が書いてある先しか見張らない。**
- 見張る前に、まだ起こしていなければ**自分で起こす**。
  `jules` → `jules` ラベルを貼る／`codex` → `@codex` とコメントする（どちらも実測で確かめた作法・無料）。
  **起こすのは1回だけ。催促はしない。**記録は `status/969/woke.json`。
- 起こせなかった先・引き金の無い先は **見張らない**（見張ると走った回数だけ永久に増える）。
  理由は `status/969/blocked.json` に書いて、台帳の赤に出る。
- `ai-kaigi#1` / `#3` は **`wake` を空にして外した**。あの板には bot が1体も居ない（実測）。
  起こせる相手を入れられた日に `wake` を書けば、そのとき自動で起こして見張りに戻る。

### ★実測：本番で効いた（2026-09-23 00:13、Macの5分便が回った）

```
00:13:13  #446 を起こしました（jules ラベル・HTTP 200）
00:13:14  #446 を起こしました（@codex コメント・HTTP 201）
00:13:14  見張りません：ai-kaigi#1／#3（bot が居ないので外した）
00:13:15  1件をページに足しました
```

**22秒で2体とも返事をした。**81回走って0件だったものが、起こした瞬間に動いた。

- `google-labs-jules[bot]` … 「Jules is on it」＋作業URL。**いま動いています。**
- `chatgpt-codex-connector[bot]` … 「You have reached your Codex usage limits」。
  ＝**Codex側の枠切れ**。こちらの不具合ではない。枠が戻れば動く。

### ★その場で出た新しい不具合も、その場で潰した

起こすために自分が書いた `@codex 上のお題をお願いします。` が、
**「tamago2022 からの返事」として961番のページに載った。**自分の声を自分で拾った
＝いちばんたちの悪い「取れた」の嘘。`<!-- tamago-factory -->` の印は、相手の連携アプリに
読み飛ばされないよう**わざと付けていない**ので、印だけでは止まらなかった。

直し方（**印だけに頼るのをやめて、3重にした**）：

1. 印（`<!-- tamago-factory -->`）
2. **起こすときに書いたコメントのidを控えて、そのidは拾わない**（`woke.json` の `commentId`）
3. **投稿者がリポジトリの持ち主（tamago2022）なら拾わない**（外部AIの返事だけを拾うのが仕事）

載ってしまった1件はページから取り消し済み。残っているのは bot 2体の返事だけ。

**次のセッションが確かめること**：

```bash
cat status/969/woke.json ; tail -5 status/969/pickup.log
grep -c 'class="panel reply"' share/check/961-talking-avatar-jirei.html   # → 2（bot2体ぶん）
```

Jules が書き終えたら3件目が自動で載ります。

---

## 4. 残った赤 9件と、その理由（直せないもの。隠していない）

| 赤 | 理由 | 誰が外せるか |
|---|---|---|
| 稼ぎ（工場本体・直近6時間 走った148本→取れた0本） | **Claudeのログインが切れている**（OAuth session expired・更新用の鍵ごと無効） | ★たまごさんだけ。アプリで1回ログインし直すと自動で再開 |
| LINE審査結果の見張り | Gmailを読む鍵が `/Users/mac/.tamago/gmail_app_password` に無い | ★たまごさんだけ |
| LINE問い合わせ返信の見張り | 同上 | ★たまごさんだけ |
| Anthropic返信の見張り | 同上 | ★たまごさんだけ |
| 外部連絡窓口の見張り | 同上（1回知らせて止まっている） | ★たまごさんだけ |
| 外部AI Grok | x.ai が team_blocked／残高なしで403。**課金がいる** | ★たまごさんの判断 |
| 外部AI Genspark | 起こす手段が無い（APIは名前解決せず・掲示板は無反応）。Genspark側の画面で1回仕込む必要 | ★たまごさんの1回の操作 |
| 外部AI GitHub Copilot | @copilot 無反応・割り当て候補に居ない・API 403。**有料プランがいる** | ★たまごさんの判断 |
| ~~アバターの返信拾い（969）~~ | **潰した。**起こしたら22秒で bot 2体が返事。いま緑 | ― |

**Gmailの鍵1つで4本が同時に開く。**ここが一番効きます。

★**有料プラン契約・課金には一切手を付けていません。**

---

## 5. 触っていないもの

- Brave（`88704cda-…`）には触っていない。
- 新しい定期タスクは1つも作っていない（全部 `tools/` の中。呼ばれるだけで自分では走らない）。
- `status/kagi_daicho_counts.json` は実測のたびに戻した（サンドボックスの時計で工場の数を壊さないため）。

---

## 6. 次の一手（順番）

1. たまごさんがClaudeにログインし直す → 工場が動き出す（ここが全部の親）。
2. たまごさんが `gmail_app_password` を置く → 見張り4本が30分以内に自動で開く。
3. 次のセッション：`python3 tools/hantei.py` を回して、969の `woke.json` を見る。
   赤が9件→減っていれば、この直しは効いている。**減っていなければ、原因を実測で掘る（「たぶん」で直さない）。**
