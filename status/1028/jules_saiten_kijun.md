# Julesの試験 ― 採点基準（★投げる前に確定。後から動かさない）

確定時刻：2026-09-23 10:45（Issueを立てる前）

## 題
アーティスト名の読み（カタカナ）赤149件を埋める。
対象149件は `status/1028/jules_shiken_149.json` に全部書いてある。**選ぶ判断はJulesに一切させない。**

## 出力形式（1つに限定）
`tamago2022/joy-relief-station` に、`yomi-answers/jules.json` を**1ファイルだけ**足すPR。

## 門（1つでも外したら ✕）
1. 変更ファイルが `yomi-answers/jules.json` の1本だけ
2. JSONとして読める
3. キー集合が149件と完全一致（過不足0・1文字も変えない）

## 合格率の数え方（機械のみ・AIを呼ばない・0円）
1件が「通った」＝ 値が空でない かつ 値が `^[゠-ヿーー・]+$` を満たす。
合格率 ＝ 通った件数 ÷ 149。

## 採点
| 印 | 条件 |
|---|---|
| ◯ 採用 | 門を全部通り、**合格率 > 54.5%（＝82件以上）** |
| △ 条件付き | 門は通るが合格率 54.5%以下、または指示の一部を外す（ファイル名違い・余計な変更） |
| ✕ クビ | 門を外す／PRが来ない |

54.5% の出どころ：人（Claude子セッション）の自己検品 310件中169件＝54.5%（1027b引き継ぎ §3-1）。

## 世話のコスト
投げてから結果が出るまでに、こちら（Cowork）が**手を出した回数**を数える。
0回＝世話がいらない。3回以上＝採用してもコストが勝つ。
※「投げる」そのもの（Issueを立てる1回）は数に入れない。

## 判定する道具
`tools/1028_jules_saiten.py`（AIを1回も呼ばない・課金0）
