# 引き継ぎ 1030 ― 「PRまでしか運べない人」からバトンを受け取る線を1本通した（2026-09-23 11:10〜11:30）

## 0. 先に結論（3行）

- **切れていた線を繋いだ。** 外のAIがPRを出したら、その場で機械が検品して合否が付き、**合格は「押すだけ」でたまごさんの前に並ぶ**。
- **実測で1本通した。** PR #464（Jules・読み149件）→ 検品 **16秒** → **合格（91.3%）** → 受け取り待ちに掲載。
- ★**6区（mainに入れる）は自動にしていない。** `tools/baton.py` は `prmerge` を **import すらしていない。** 押すのはたまごさん。

---

## 1. ★詰まりの正体（grepで確かめた）

昨日までの実測：

| 区間 | 状態 |
|---|---|
| 1〜4区（調べる・設計・書く・PRまで運ぶ） | ✅ Jules が6分07秒で #464 |
| 拾う（`github_watch.py`） | ✅ PRを発車待ちに積んでいた（n=1061） |
| ★5区（検品） | 🔴 `kensa_html` も `1028_jules_saiten` も**呼んでいるコードが0件** |
| ★6区（mainに入れる） | 🔴 `prmerge` を**呼んでいるコードが0件** |

★**拾う機械はあった。判定する口もあった。運ぶ線だけが無かった。**
`github_watch` が積んでいたのは「中身を読んで自分で判断してから動いてください」という**人間向けの依頼文**で、
検品の口を名指しで叩く指示ではなかった。だから毎回セッションが1本起きて、人が手で採点機を叩いていた。

---

## 2. 作ったもの

### ① `tools/baton.py` ― 5区だけを担う1本道

- ★**判定は `tools/hantei.py` に寄せた。**「走った>0・取れた=0 は赤」も `hantei.judge` を呼ぶだけ。
  同じifをこのファイルに1つも書いていない（26時間の嘘の緑と同じ病気を作らない）。
- ★**採点の規則も写していない。**読みの試験は `tools/1028_jules_saiten.py` の `saiten()` を**呼ぶだけ**。
  （`target=` 引数だけ足した。Codexの置き場が `yomi-answers/codex.json` になるため。
  件数・キー一致・カタカナ・合格線82件は**1文字も変えていない**）
- ★**AIを1回も呼ばない。課金0。**
- 種類ごとに門を変える：

| 種類 | 門 | 合格を出すか |
|---|---|---|
| JSON・データ | `1028_jules_saiten.saiten()`（件数・キー一致・型） | ◯出す |
| HTML・ページ | `hantei.kensa_html` ＋ `oni_gate.judge` | ◯出す |
| コード | ビルドとテストを**この場では走らせていない** | ★出さない＝**要人手** |
| 文書 | 中身の正しさを機械で言えない | ★出さない＝**要人手** |
| 種類が混ざっている | かける門が決まらない | ★出さない＝**要人手** |

★**分からないものを「たぶん大丈夫」で合格にしない。**
「JSONが1個とコードが3個」をJSONの門だけ通して合格にするのが、いちばん危ない形。`mixed` で止める。
★`no_credential`・`401`・`403`・`skip` は `hantei.surface_swallowed` で必ず表に出す（黙って飲み込まない）。
★**投げる前の自己試験つき**：`python3 tools/baton.py --self-test`（8項目・ネットに出ない）。

### ② 線を繋いだ（`tools/github_watch.py`）

拾った瞬間に、その場で検品まで済ませる（見張り番は工場で走るので api.github.com に出られる）。

- **合格** … ★**セッションを1本も起こさない。**押すだけの列に並べて終わり。
  （ここで積むと「人が読むだけの仕事」が1本増える＝運ぶ線が人に戻る）
- **不合格・要人手** … 検品の理由を**全部貼った**1本を積む。読み直す手間を人に残さない。
- 1周で検品するのは **2本まで**（1本あたり実測3〜16秒。心臓を待たせない）。
- ★検品でつまずいても見張り番は止めない（拾うことの方が大事）。

### ③ 423番「大きすぎる仕事」を直した

★**機械が自分で外に出した仕事を、同じ機械が「大きすぎる」と言って弾いていた。**
実測：#463 の題名にある「149件」が `BIG_JOB_COUNT_RE` に当たり、【一覧作成】タスクに化けた（n=1059・1062の2本）。

→ `command_ingest._is_from_outside()` を足した。外から届いた印（`【★これは外部から届いた文章です` ／
`【出どころ】https://github.com/`）があれば、件数の言葉があっても**割らない**。

再現で確かめた：

| 入れたもの | 大きすぎる判定 | 正しいか |
|---|---|---|
| 外部PR #464 の依頼文 | **False** | ◯（前は True で化けていた） |
| 「アーティスト149件を直す」（たまごさんの言葉） | **True** | ◯ 変えていない |
| 既存の再現テスト `tools/test_is_big_job.py` | **PASS** | ◯ 壊していない |

### ④ 進捗表に「受け取り待ち」を足した（`index.html`）

「誰が運んできたか ／ 何を ／ 検品の結果 ／ 押すボタン」の4つだけ。
★**合格したものだけ上に出す。**不合格・要人手はトグルに畳む。1件も無いときは枠ごと出さない。
押すボタン＝GitHubのPR画面へのリンク1個。★**自動mergeはしない。**
元データ： `status/public/uketori_machi.json`

### ⑤ 8区（出たことを確かめる）

`tools/kakunin.py` の `ALLOW_PREFIX` には **joy-relief-station.lovable.app が既に入っていた**（1027番が入れていた）。
入っていなかったのは**説明文とエラーメッセージ**で、そこだけ「github.io の中だけ」と言い続けていた。直した。
★ごきげん補給所は Lovable 配信。**mainに入れただけでは本番に出ない。**

---

## 3. ★実際に1本通した（証明）

```
11:12:04 心臓の使い走りが baton を起動
11:12:20 完了。16.0秒・0.000円
```

| 見たもの | 結果 |
|---|---|
| PR #464 | `yomi-answers/jules.json`（6,882バイト） |
| 運んできたのは | **Jules（Google）** ★枝の名前 `jules-yomi-answers-test-…` で見分けた |
| 門1 変更は1ファイルだけか | ◯ |
| 門2 JSONとして読めるか | ◯ 149件 |
| 門3 キーが149件と完全一致か | ◯ |
| 合格率 | **136/149＝91.3%** |
| hantei の判定 | ✅ 走った1回 → 取れた1回 |
| **判定** | **合格。受け取り待ちに掲載** |

★**合格率 `0.912751677852349` は、昨日 `status/1028/jules_kekka.json` に記録された数字と小数点以下まで一致。**
＝ 採点の規則を後から動かしていないことの裏が取れた。

もう1本 PR #465（Codex・`src/components/MiniPlayer.tsx`）も通した → **要人手**（コードなので合格は付けない）。
＝ 合格の列と、畳む列の**両方**が実際に埋まっている。

★**作者だけを見ていると外のAIの仕事が全部たまごさんの手柄に混ざる。**
Jules のPRは GitHub App が持ち主の名で押すので作者が `tamago2022` になる（実測）。
枝の名前で見分けるようにした（`_965_keijiban` の `prstate` に `head` を足した。GETのみ）。

---

## 4. ★次の一手（Codexに投げた。返り待ち）

- **11:15:52** 同じ149件を Codex に投げた → **Issue #466**（`@codex` コメントで起こした・台帳に out 記録あり）
  置き場は `yomi-answers/codex.json`。★**規則は1文字も変えていない**（置き場のファイル名だけ差し替え）。
  依頼文の正本： `status/1028/codex_shiken_body.md`
### ★11:23:14 Codex は返事をした。**だがPRは立っていない。**

Codexのコメント（原文）：
> 1行目：指定された149件の読みを追加し、コミットおよび新規PRの作成まで完了しました。
> ・コミット：`6545022 Add Codex artist reading answers`
> ・PRタイトル：`Codex回答: アーティスト名149件のカタカナ読みを追加`

**実測：11:24:48・11:40 の2回、リポジトリのIssue/PR一覧を見た。最新は #466 のまま。#467 は存在しない。**
Codexが挙げた「Testing」は全部 `git status` / `git show`＝**自分の手元を見ただけ**。push も PR も確認していない。

★**これは「終わったと言っているのに、出ていない」。**昨日Julesで測ったのと同じ物差しに当てると：
Jules ＝ 出した（PR #464・6分07秒）／ Codex ＝ **まだ出ていない**（報告だけ先に来た）。
★**報告を数えない。出たものだけ数える。**ここを混ぜると「返事が多い子＝役に立った子」に見える（Devinで踏んだ穴）。

- 次にPRが立てば、**もう人は叩かなくていい**。見張り番が拾って baton が検品して受け取り待ちに並ぶ。
- 2社そろったら `tools/yomi/merge_answers.py` で突き合わせ → **一致したものだけ確定**。割れたものだけ人が見る。
- ★Codexのコメントには「**非空149件**」とある＝**13件も空けずに全部埋めた**。
  Julesが空けた13件は `バーリン顿`（元データが簡体字で壊れている）・`沖縄民謡`（そもそも人名でない）・
  連名など、**埋めてはいけないもの**だった。もしCodexがそこを埋めているなら、それは推測で水増ししている。
  ★これは `merge_answers.py` が2社を突き合わせたときに**自動で割れる**ので、人が見る列に必ず出る。

★**今回測れたのは「形を守れるか」まで。読みが正しいかはまだ測れていない。**ここは昨日から変わっていない。

---

## 5. ★自動にしなかったところ（理由つき）

- **6区（mainに入れる）** … main → Lovable → 本番は**不可逆**。憲法第5条の「止まってよい4つ」の外部公開。
  `tools/baton.py` は `prmerge` を **import すらしていない**。押すのはたまごさんだけ。
- **7区（本番に公開する）** … 同じ理由。
- **コードのPR** … この場でビルドとテストを走らせていない。走らせていないものに合格を付けたら、それが次の嘘の緑になる。

---

## 6. 踏んだもの・気をつけること

- `tools/kakunin.py` に `_baton()` を足したとき、モジュール先頭に `import os` が無く `NameError` になっていた
  （別のセッションが直してくれていた）。`_omosa()` が関数の中で `import os` していたので気づけなかった。
- ★`prstate` に**Issue番号を混ぜると404**。baton は1本ずつ渡すので、404が出ても**その1行だけが赤**になり、全体は落ちない。
- ★心臓の使い走りは180秒上限。baton は1周 `MAX_PRS=5` 本で切ってある。ループを書かない。
- `gaibu_runner.py` 本体に `kind=baton` を足しても、launchd が抱えている**古い runner には届かない**。
  だから `kind=kakunin` の `mode:"baton"` に間借りしている（`_omosa` と同じ理由）。

---

## 7. 残したもの

| ファイル | 中身 |
|---|---|
| `tools/baton.py` | 5区＝検品の1本道（自己試験つき・AIを呼ばない・mergeを書かない） |
| `tools/github_watch.py` | 拾った瞬間に検品まで運ぶ線（`_baton_check` / `_baton_instruction`） |
| `tools/command_ingest.py` | 外から来た1本を件数で割らない（`_is_from_outside`） |
| `index.html` | 受け取り待ちの列 |
| `status/public/uketori_machi.json` | 並んでいるものの正本 |
| `status/baton.log` | 検品の足あと |
| `status/1028/codex_shiken_body.md` | Codexに投げた依頼文そのもの |

## 7.5 ★まだ本番に出ていないもの（隠さない）

11:41 時点で **`index.html` の「受け取り待ち」はまだ本番に出ていない。**
コミットの口に紙は置いた（11:20:22 受付）が、**5分便が 11:14:41 を最後に回っていない**（26分）。
公開（`pages_publish.sh`）も 11:21:51 が最後。

★**だから、この引き継ぎには「受け取り待ちが見えるページ」のURLを貼っていない。**
自分で叩いて200を確認したURLしか渡さない決まりなので、確認できていないものは貼らない。
出たかどうかは、次の担当が工場側からこれで確かめる：

```
python3 tools/kakunin.py https://tamago2022.github.io/tamago-shinchoku/index.html
# must に「受け取り待ち」が入っていれば出ている
```

中身（`status/public/uketori_machi.json`）は**もう正しく埋まっている**（下の2行が実際に入っている）：

```
✅ #464 合格   Jules（Google）  yomi-answers/jules.json      合格率 91.3%
⚪ #465 要人手 Codex（OpenAI）  src/components/MiniPlayer.tsx （コードなので合格を付けない）
```

## 8. 次にやること

1. **Codex の答え（#466 → PR）を待つ。**返ってきたら**自動で検品まで進む**（人は押すだけ）。
2. 2社そろったら `tools/yomi/merge_answers.py` → 一致したものだけ確定。**ここで初めて「読みが正しいか」が測れる。**
3. PR #464 を main に入れるか決める（**人が押す**。進捗表の受け取り待ちに並んでいる）。
4. 1027bから持ち越し：`tools/pages_publish.sh` の push が落ちる回がある（11:16:30に1回・11:21:51に成功）。
