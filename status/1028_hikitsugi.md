# 引き継ぎ 1028 ―「重い」を数字にする物差しを作った（2026-09-23 10:40〜）

## 0. 先に結論（3行）

- **今回は軽くしていない。「重さを測る物差し」を作った。**
  理由：物差しが無いと「軽くしました」が本当かどうか誰にも分からない。今日だけで嘘の緑を何件も見た。
- **トップが送ってくる量＝約2.3〜3.0MB／34本。一番でかいのは `youtube-id-map-*.js`（gzip後 約820KB）。**
  ★**Devinが過去2回当てた見立ては、今日叩き直しても当たっていた。**（ただし2位に mp4 727KB が居る）
- **物差しは `tools/omosa.mjs`。`node tools/omosa.mjs` でいつでも同じ手順で測り直せる。**
  進捗表の1行は `tools/hantei.py` の `omosa()`。**前回より5%超重くなったら赤。**

---

## 1. ★作ったもの（4つ。全部 tools/ の中。新しい常駐は0個）

| ファイル | 何をするか |
|---|---|
| `tools/omosa.mjs` | **物差し本体。** headless Chrome を CDP で動かして実測する。課金0・GETのみ |
| `tools/omosa_target.json` | **何を測るかの設定。** URL・試行回数・幅。ここだけ直せば測り直せる |
| `tools/omosa_page.py` | `status/omosa_last.json` → `share/check/1028-omosa.html` を作る。**数字は手打ちしない** |
| `tools/hantei.py` の `omosa()` | 進捗表の1行。前回比+5%超で🔴。`audit()` に入れたので自動で出る |

呼び方（サンドボックスから工場に頼む）:

```python
gaibu_kuchi.enqueue_job("kakunin", {"mode": "omosa", "runs": 20, "timeout": 1800})
```

### ★なぜ `kind=kakunin` に間借りしたか（runnerに kind を足さなかった理由）
`gaibu_runner.py` は **kind=kakunin のとき毎回 `importlib.reload(kakunin)`** する。
＝ launchd が抱えている**古い runner を止めずに**、`kakunin.py` に関数を足すだけで今すぐ工場で動く。
runner 本体に kind を足しても、**走り続けている古い runner には届かない。**
（同じ理由で 1030番も `_baton` をここに足していた。`os` の import が抜けていたので足した。）

---

## 2. ★実測（2026-09-23・`https://joy-relief-station.lovable.app/`）

### ① トップが送ってくる量
- **合計 約2.3〜3.0MB／通信34本**（キャッシュ切り・1280px・gzip後の実際に線を流れる量）
- 内訳：**JS 1.47MB／動画 727KB／画像 816KB／CSS 34KB／HTML 8KB**
- **一番でかい1つ＝`assets/youtube-id-map-D3kTqsFk.js` 838,724バイト（約820KB）。JS全体の57%。**
- 2位＝`media/joy-relief-footer-loop.mp4` 727KB（206で流れ続ける）
- 3位＝`assets/index-*.js` 573KB

### ★①のおまけ：トップは「読み込みが終わらない」
`readyState` が20秒たっても `interactive` のまま `complete` にならない。
犯人は繰り返し再生の `joy-relief-footer-loop.mp4`。
＝ **ブラウザの「読み込み中」がいつまでも消えない。体感の『重い』の正体はここの可能性が高い。**
（★これは実測した事実。「だから直せば軽くなる」は**まだ確かめていない**ので書かない。）

### ② 棚編集の反応
- 棚編集は**ボタンではなく `/admin/shelves` という1枚の画面**だった。
- **鍵なしでも画面は出る（ログイン壁は無い）。** ただし中身が**「読み込み中…」のまま来ない。**
- ＝ たまごさんの「棚編集が重い」は、押した後の反応ではなく**開いた後ずっと待たされること。**
- ★**鍵を使う測定はしていない**（物差しは課金0・鍵なしで誰でも回せる形にしたかった）。
  鍵を使えば違う結果になるかもしれない。**そこは確かめていない。**

### ③ URLを貼って落ちる
★**たまごさんの言葉は2通りに読める。決めつけずに両方を箱にした。**
- **(あ) 棚編集の欄にYouTubeのURLを貼る** … ②のとおり棚編集の中身が出てこないので**たどり着けず、回数が出ていない。**
  ★**「落ちなかった」ではない。「測れていない」。** 数える箱は出来ているので、中に入れれば数字が出る。
- **(い) 曲ページのURLを開く** … `openUrls` の4本を順に runs 回。鍵なしで回数が出る。

★**一度やらかしかけた事故（残しておく）**
最初、貼り先を「URLっぽい欄が無ければ最初の入力欄」にしていたら、
**どのページにも居る足元の「ひとこと目安箱（感想の箱）」が毎回選ばれていた。**
＝ **別物を20回叩いて『URL貼りは落ちませんでした』と書くところだった。**
今は**URLっぽい欄が無ければ諦める**。違うものを測って数字を出すのが一番たちが悪い。

### ④ スマホ幅（375px）
同じ4つを375pxでも測る（`widths: [1280, 375]`）。横に流れていないかも見る。

---

## 2-5. ★途中で見つけた本物の障害（次の人が必ずぶつかる）

### (a) 工場の runner に「1件90秒で打ち切り」が入った（今日のどこか）
`status/gaibu_runner.log` に **`⏱ 打ち切り … 上限90秒`** が出るようになっている。
＝ **90秒を超える仕事は問答無用で殺される。** 重さの測定は素で2〜15分かかるので全滅した。
→ 対策として **`--phase` で小分けにできるようにした**（`bytes` / `edit` / `open` / `paste`）。
結果は `status/omosa_last.json` に**上書きではなく足し込む**ので、3便に分けて測れる。

```python
gaibu_kuchi.enqueue_job("kakunin", {"mode":"omosa","phase":"bytes","width":1280,"timeout":86})
```

### (b) 打ち切られた測定が headless Chrome を残し、次の測定が全滅する
「Chromeのデバッグ口が開きませんでした」が連発する。
→ `omosa.mjs` の `souji()` が起動時に片付けるようにした。
★**殺すのは `--user-data-dir` に `omosa-` を含むものだけ**＝この道具が作った一時プロファイル。
**たまごさんが普段使っているChromeには絶対に触らない。**
★ただし **12:04時点でもまだ起動に失敗している。**（原因未特定。機械が混んでいる可能性）

### (c) ★一番大事：「サイトが落ちた」と「測定装置がコケた」を混ぜかけた
`openOnce()` の最初の版は、**こちらのChromeが起動できなかった回まで「落ちた」に数えていた。**
＝ **サイトのせいではない回数を混ぜた落ち率**という、新しい嘘が出るところだった。
→ `MEASURE_FAIL` で見分けて、**測定失敗は分母からも外す**ようにした。
★この手の混入は、物差しそのものを信用できなくする。**次の人も必ず分けること。**

---

## 3. ★次の人へ（重要な順）

0. ★**出したページ：** https://tamago2022.github.io/tamago-shinchoku/share/check/1028-omosa.html
   （12:06 に自分で叩いて **200・`</html>`・中身あり** を確認済み。`putfile` の口から出した）
   数字の出どころは `status/omosa_last.json` の `_源` に**仕事票のIDで**書いてある。手打ちの数字は無い。
1. **まだ何も軽くしていない。** ここからが工事。**先に `node tools/omosa.mjs` で今の目盛りを取ってから触ること。**
   触った後にもう一度測れば、**進捗表が自動で「軽くなった／重くなった」を言う。**
2. **一番でかいのは `youtube-id-map-*.js` 820KB。** ただし**それを消せば軽くなる、とはまだ確かめていない。**
   （トップで本当に要るのか／遅延読み込みにできるのか、を見るのが次の1手）
3. **棚編集の中に入る道を作ると③(あ)の数字が出る。** 鍵をどう扱うかは決めていない。
4. `tools/pages_publish.sh` は1027番の指摘どおり**09:25を最後に完走していない。**
   このページも `_965_keijiban.py` の `putfile` から出した。

## 4. 触っていないもの（約束どおり）
- 新しい定期タスク（`mcp__scheduled-tasks__*`）は**1つも作っていない。**
- 確認ポップアップの出る口は**1つも呼んでいない。**
- Brave には触っていない。ブラウザ拡張も使っていない（工場のheadless Chromeのみ・一時プロファイル・使用後SIGKILL）。
- `joy-relief-station` を公開にしていない。`[skip ci]` を入れていない。課金0。
