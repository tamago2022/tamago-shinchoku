# worktree q720-0904 調査メモ（10ターン上限で打ち切り・簡易まとめ）

作業ディレクトリ: `/Users/mac/Documents/AI作業/.worktrees/q720-0904`
ブランチ: `claude/q720-0904`（git status clean、独自コミット無し。git log先頭はwhiteboard自動書き戻し等の共通コミットのみ）

## 調査結果

- **対象Issue「#720」**：GitHub CLI(`gh`)が本環境に未インストールのため`gh issue view`は実行不可（確認済み事実）。リポジトリ内`grep`（`.claude/`, `ai-brain/`, `git log --all --oneline`）でも「#720」「案件#720」「q720」に一致する記録・issue-briefsは発見できず（確認済み事実＝記録なし）。worktree名の「720」がGitHub issue番号を指すのか、別の採番（作業キュー番号等）なのかは未確定（推測が必要な部分）。
- ローカルの`.claude/ACTIVE_WORK.md`内で「720」がヒットしたのは`scripts/bulk-ingest.ts`のコード内行番号への言及のみで、Issue #720とは無関係（確認済み事実）。

## 1. 今回守るべき既存ルール（AGENTS.md / CLAUDE.md 確認済み）

- 第一条・自律走行：確認義務①〜⑦（不可逆操作／外部公開／金銭／秘密情報／権限・アクセス制御／法務・規約・プライバシー／北極星変更）以外は聞かずに決めて進める。例外的判断でも店主に直接聞かず`.claude/PENDING_DECISIONS.md`へ1行書いて先に進む。
- 仕入れ（新規ページ作成・DB登録）は自律OK、**既存「棚」への追加（キュレーション・掲載判断）は店主の明示指示がある時だけ**（実例：カレー棚への無断2件追加事故）。
- **ページのフォーマット・デザイン変更（複数ページ共有コンポーネント）は本番反映前に必ず店主確認**（PENDING_DECISIONSへ記載してから待つ）。実装・PR作成までは自律可。
- 第三条：ブラウザ操作は`select_browser`でdeviceId `93fefeab-0797-497c-ae03-6dd353624681`固定・tabId使い回し・使用後即`tabs_close_mcp`。
- 第四条：完走がデフォルト。PR作成〜mainマージ〜Lovable反映・Publish押下まで自分で完走。店主に「マージしてください」「公開してください」は禁止。
- 個人アーティストのプロフィールに生年を書かない（バンドの結成年は可）。
- AIカバーは表記でAI明示＋原曲への双方向リンク必須。
- 作業原則：型チェック・ビルド・テスト実行、失敗を残してmainへ進めない。生成ファイルは生成元を直す（直接編集しない）。

## 2. 過去に起きた類似事故（AGENTS.md / CLAUDE.md 記載の実例、確認済み事実）

- カレー棚にAIが無断で2件追加（棚キュレーションの無断判断事故）。
- 2026-08-14、アーティストページ（Corleo・美空ひばり等）が意図せず異なるフォーマットへ書き換わる事故（共有コンポーネント変更の無断本番反映）。
- akikoさんのプロフィールに生年「1976年生まれ」記載、本人指摘で削除・ルール化。
- 「Project KOBITO」セッションが無関係な作業をして店主が見失った事故（1セッション1ミッション違反）。
- 既存メモリ記録（`.claude/agent-memory/tamago-memory/MEMORY.md`）：CORLEO/Dom Corleo混入（データ未分離）、気分診断Enyaのアーティスト単位重複除外漏れ、文脈適合性の機械検品が無い、プレイリスト機能はpush済みだが本番未反映だった時期がある、ログインなしlocalStorageのみのパーソナライズ。

## 3. 触るべき生成元と、触ってはいけない生成ファイル

- 原則：「生成ファイルは生成元を直す」（AGENTS.md作業原則）。本worktreeでは対象Issueが特定できなかったため、**今回の作業で実際にどのファイルが生成元／生成ファイルに該当するかは記録なし**（対象Issueの中身が未特定のため）。
- 一般原則として`coverGuide.ts`等の生成元データは「守る資産」（`ai-brain/pm_brief.md`）に明記。既存ID・棚ID・URL・カード間リンクも守る資産。

## 4. 既存データとの重複・競合リスク

- 対象Issueが未特定のため具体的な重複・競合対象は記録なし。
- 一般的な既知リスクとして、メモリに記録済み：CORLEO/Dom Corleo混入、気分診断でのアーティスト単位重複除外漏れ、文脈ズレ検品の機械見張り不在。

## 5. 店主が最終的に確認する画面と状態

- 完了の4段階（AGENTS.md／pm_brief.md）：①実装・検品 ②PR作成 ③mainへMerged ④Lovableへ反映。⑤店主が実画面で合格。AIは④までを自動追跡し、⑤は「店主確認待ち」として明示する。
- 本番URL・対象画面は対象Issue未特定のため記録なし。

## 未解決・持ち越し事項

- 「Issue #720」の実体（GitHub issue番号か、社内採番か）は今回特定できず。次セッションはまず`gh`コマンドの利用可否（インストール）または本リポジトリのissue管理方式（`ai-brain`配下のissue-briefs等）を再確認することを推奨。
