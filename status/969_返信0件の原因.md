# 969番「アバターの返信拾いが81回走って0件」の原因（2026-09-22 実測）

**結論：拾う側は壊れていない。拾う先に、外部AIの返事が1件も存在しない。**
977番の外部AIと**まったく同じ病気**。「呼んでいない相手を待っていた」。

直すのは次のセッションでよい（たまごさん指示）。ここでは原因だけ確定させる。

---

## 1. 見張っている先と、そこに実際にあるもの（GitHub API 直叩き）

`tools/_969_avatar_reply_pickup.py` が見ているのは次の3つ（`status/969/issue.json` と `targets.json`）。

| 見張り先 | 実際のコメント数 | 中身 |
|---|---|---|
| joy-relief-station **#446** | **1件** | 書いたのは **tamago2022 本人**。本文に `<!-- tamago-factory -->` が入っている |
| ai-kaigi **#1** | **0件** | — |
| ai-kaigi **#3** | **0件** | — |

外部AIが書いたコメントは **3つ合わせて0件**。

## 2. 「1件あるのに0件」は、バグではなく設計どおり

#446 の唯一のコメントは工場自身が書いたもので、本文に `<!-- tamago-factory -->` が入っている。
拾い口はこの印が付いたものを**意図的に飛ばす**（自分の書き込みを自分で拾って無限に増える事故を止めるため）。

```python
if "<!-- tamago-factory -->" in (c.get("body") or ""):
    continue
```

→ **拾える返事が0件だから0件。`seen.json` の ids が空なのも正しい。**

## 3. ではなぜ返事が来ないのか（ここが本当の穴）

- **ai-kaigi には bot が1体も居ない。**入っているのは tamago2022 ただ1人（977番の実測と同一）。
  `#1` と `#3` は、読む相手が存在しない板に立てた号。ラベルを貼っても誰も来ない。
- **joy-relief-station #446 には、起こす引き金が何も付いていない。**
  - Jules を起こすのは `jules` ラベル（公式ドキュメント＋実測）→ **付いていない**
  - Codex を起こすのは `@codex` コメント（実測）→ **打っていない**
  - Devin は別経路（API）→ 投げていない

つまり **「投げた」つもりで、誰も起こしていなかった。**

## 4. 次に直すときの一手（今回は手を付けない）

1. #446 に `jules` ラベルを貼る／`@codex` コメントを打つ（`tools/nageru.py` が既に持っている作法）。
   ＝新しい号を立て直すより安い。
2. `targets.json` から **ai-kaigi#1 / #3 を外す**。bot が居ない板を見張り続けても永久に0件で、
   「走った回数」だけが増えて赤が消えない。
3. ★根っこ：**「見張る先を足すとき、そこに起こす引き金があるかを確かめる」**を関所にする。
   977番と969番で2回同じ事故が起きている＝個人の落ち度ではなく仕組みの不在（憲法20条）。

## 5. ついでに分かったこと（②に効く）

Mac上の棚 `~/Desktop/joy-relief-station/src/lib/coverGuide.ts` は **2026-09-09 更新・6,326,752バイト**。
いっぽう率の計算に使っている `status/_936/keep/.../coverGuide.ts` は **09-18・6,495,394バイト**で、
**スナップショットのほうが新しい。**Macのチェックアウトが9日ぶん古い。
率はスナップショット側で数えるのが正しい（今そうなっている）。Mac側を `git pull` するのは別件。
