# XAI_API_KEY を貼った後にやること（2026-09-18）

## 貼る場所
Lovable プロジェクト `8ebdb648-3686-4457-b42c-d01c493793b1`
→ その他（More） → クラウド（Cloud） → Secrets
→ 「Add secret」 → Name に `XAI_API_KEY` → 右の Value 欄に貼って Save
（同名を入れれば上書きされる）

## 貼る値
xAIコンソール https://console.x.ai/team/69b11444-99e0-474b-b888-8598da9f8eed の API Keys。
**使用実績のある `momotaro` を推奨**（`joy-relief-station-voice` は一度も使われていない）。

## 貼り終わった後の手順（こちらで実行する）
1. Lovable で「公開（Publish）」を押す
2. `https://eecdooahvromxykbldud.supabase.co/functions/v1/voice-session` を叩いて **403が消えたか実測**
3. 本番 `/cover-guide` を開いて「話す」を押す → 音声が始まるか確認
4. 複数ターン会話して途切れないか確認

## 今回の詰まり
Chrome（deviceId `7d965dae`）が Claude in Chrome に未接続。
接続されているのは Brave（`88704cda`）のみで、これは触らない指示。
→ Chrome側で Claude拡張をつなぎ直せば、こちらから画面を開ける。
