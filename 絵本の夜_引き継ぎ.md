# 絵本の夜（canvas手描きアニメ3本）引き継ぎ

## 現物
- 目次 https://tamago2022.github.io/tamago-shinchoku/share/check/ehon-index.html
- ① https://tamago2022.github.io/tamago-shinchoku/share/check/ehon-01-hanabi.html
- ② https://tamago2022.github.io/tamago-shinchoku/share/check/ehon-02-hokyujo.html
- ③ https://tamago2022.github.io/tamago-shinchoku/share/check/ehon-03-tamago.html
- 全部 200 確認済み（2026-09-23）。375px でも確認済み。
- 画面をタップ／クリックで1回だけ動く。終わると止まる。勝手にループしない。

## 直し方
HTMLを直接さわらない。`tools/ehon_build.py` を直して
`python3 tools/ehon_build.py` を叩くと share/check/ の3枚が書き変わる（目次は手書きHTML）。

## 棒人間にならなかった理由（ここが肝）
1. 直線を1本も引いていない。全ての線を `densify` で細かく割り、各点に Perlin ノイズで
   ±2〜3px の乱れを足し、`quadraticCurveTo` で繋ぎ、**同じ線を2〜3回ちがう乱れ方で重ね描き**（`hand`）。
   面も同じで、塗ったあと縁を2回なぞる（`handFill`）。稜線には短い線を刺す（`hatch`）。
2. 紙の粒を1枚かぶせる（`grainTile`）。静止画層に焼き込んであるので毎フレームの負荷はゼロ。
3. 色は5色だけ。`C` の紺・生成り・橙・深緑・黒。**グラデーション・発光・虹色は1つも使っていない。**
4. 動きは ease-out 系だけ（`outCubic` / `outQuint`）。等速で動かさない。
5. 顔は点2つと弧1つ（`face`）。描き込まない。
6. 生き物は描いていない。家・木・棚・卵・光だけで成立させている。

## 作り
- 静止画は `stat` オフスクリーンに1回だけ描き、毎フレームは `drawImage` 1発＋動く粒だけ。
- 描画時間（ソフトウェア描画・375x812・180フレーム実測）
  ① avg 4.4ms / p95 9.5ms ② avg 2.7ms / p95 5.2ms ③ avg 1.9ms / p95 3.8ms
  → 1フレーム16.7msの枠に対して十分。実機の数字は URL の末尾に `#fps` を付けると左上に出る。
- バイト数 ① 18,059 ② 14,589 ③ 15,805 目次 2,306（全部1MB未満・外部CDNなし・1ファイル完結）

## 次にやるなら
- ②はサイトのトップ帯に置ける形にしてある（棚が右端まで抜けている）。埋め込むなら iframe で高さ固定。
- ③は案内人の背景用。卵の位置は `EG.cx` / 地面は `GY` で動かす。
