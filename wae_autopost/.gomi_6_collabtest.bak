#!/bin/bash
cd "$(dirname "$0")"
echo "■ Collabs（共同投稿）が本当に通るかを、実際にMetaに叩いて確かめます。"
echo "  投稿はしません。コンテナを1個作るだけで、24時間で勝手に消えます。"
echo ""
python3 post.py --collabtest
echo ""
echo "（Enterで閉じます）"; read -r _
