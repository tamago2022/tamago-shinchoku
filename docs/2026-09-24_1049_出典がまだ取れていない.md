# 2026-09-24・1049便／出典がまだ取れていないもの

★推測で埋めていない。**空欄のまま前に進めて、分からなかったものをここに並べる**（たまご運用OS §11-3）。

---

## 1. 別人の曲が本人の札になっている疑い（★これは直した）

| 札 | 何が書いてあったか | 何が取れなかったか |
|---|---|---|
| `Who gon' love me / NEED - Topic`<br>id `3fdfe3fd-6429-40c3-bb87-335b7d072efb` | 「渋谷すばるが刻んだ、**2020年**の体温。」＝**渋谷すばる本人の曲だと断定していた** | 渋谷すばるの2ndアルバム『NEED』（2020-11-11）の収録曲は Sing / Earth Color / BUTT / Noise / 水 / 風のうた / たかぶる / 今日はどんな一日だった / 人 / 素晴らしい世界に / Sing の11曲で、**「Who gon' love me」は入っていない**（オリコンの全曲レビュー・レーベル/本人サイトのディスコグラフィーで確認）。<br>チャンネル名の `NEED - Topic` はYouTubeが自動で作る「Topic」チャンネル。**アルバム名『NEED』と、NEED という名前の誰かが、文字の一致だけで繋がれた疑いが強い。**（akikoの棚に矢野顕子を入れた事故と同じ形） |

**この便でやったこと**：札は消していない（消すのは不可逆＝たまごさんの判断）。
コピーから **渋谷すばるという断定と「2020年」を外した**手書きの下書きを `status/copy_tegaki/` に置いた。
題名も `NEED「Who gon' love me」（YouTubeのTopicチャンネル）` に変えて、**誰の曲かを名乗らない形**にした。

**残っている宿題**：この動画を上げているのが誰なのか、識別子（チャンネルID）で取る。
取れたら本人の棚へ。取れなければ棚から外す。★名前の文字だけで決めない。

出どころ：
- <https://www.oricon.co.jp/special/55334/>（『NEED』全曲レビュー）
- <https://wmg.jp/shibutanisubaru/discography/23582>（レーベルのディスコグラフィー）
- <https://shibutanisubaru.com/news/detail/128>（本人サイト）

---

## 2. 棚ぜんたいの「別人混入の疑い」の数（実測・2026-09-24）

```
python3 tools/sekisho/gate_artist_song.py status/_1039/src_lib_coverGuide.ts --json /tmp/sekisho.json
→ blocked 0件 ／ hold 2230件 ／ collisions 389棚
```

- **blocked（曲名に「自分の名前を含む別人のフルネーム」が入っている＝即アウト）は0件。**ここは効いている。
- **hold 2230件**は「保留＝載せない側に倒す」判定。例：
  - `Divisi` の棚に `Joy Division` 由来の曲（名前が `Divisi` ⊂ `Joy Division`）
  - `ゴスペラーズ` の棚に `ゴスペラーズ with スターダスト☆レビュー` 名義
  - `Mio` の棚に `Luciano Pavarotti`、`佐藤竹善` の棚に `Blue`
- ★**この便では1件も外していない。**2230件を機械で外すのは「大量一括更新」（憲法第5条・確認が要る6操作）に当たる。
  たまごさんが「この束、外していい」と一言くれたら、`tools/sekisho/remove_foreign_songs.py` で一度に外せる（外した中身は `.moved.json` に残るので戻せる）。

---

## 3. 動画がまだ確認できていないもの

- `status/shiire_kouho/` の候補のうち、**OTYKEN周辺28件は全部 `"video": "未確認"` のまま。**
  棚に出す前に「公式チャンネルか／静止画だけでないか」を通す必要がある。
- ★サンドボックス側の回線からは YouTube にも MusicBrainz にも Wikipedia にも出られない（実測：全部 403）。
  確認できるのは **WebSearch と web_fetch を持っているセッションだけ**。この便はそれで5件ぶん進めた（→ `status/nyuka/pending/`）。

---

## 4. この便で触っていないもの（次の便へ）

- `status/nyuka/pending/` に積んだ5件は、**Mac側の便が門1（出典照合）と門5（採点）を通すまで確定しない。**
  ★いま `tools/yosan.py` の上限が全部 **0円** なので、門5（OpenAIの採点）は止まる。
  止まった分は `rc==2` で pending に残る設計なので**消えない。**栓が開いた回に通る。
- `.git/index.lock` を 02:49 に1本取り残した（サンドボックスからは消せない）。
  `tools/git_lock_reaper.py` が片づける。★この便は以後 git に触っていない。

---

## 5. 動画の中身が確かめられず、積まなかったもの（2026-09-24 追記）

| 候補 | 動画id | なぜ積まなかったか |
|---|---|---|
| THE BETHS「Expert In A Dying Field」 | `-KACt6YhOyY` | 題に `(Official)` とあるが、**それが動く映像なのか Visualizer（静止画＋波形）なのかが確かめられない。**同じ曲で `(Official Visualizer)` の別動画（`sd3CqVVWJws` / `SK72Exw2T4o`）が並んでおり、静止画だけの動画を積む危険がある。★推測で埋めず、積まずに残す。 |
| THE BETHS「Expert In A Dying Field (Live on KEXP)」 | `KMhm3j2RfsI` | 演っているのは本人で、動く映像なのも確か。ただし**アーティスト本人の公式チャンネルではなく KEXP（ラジオ局）のチャンネル。**「公式チャンネルか」の線をどう引くかが決まっていないので積まない。 |
| 石野卓球 / TAKKYU ISHINO | ― | 見つかった動画が `AUDIO ONLY` 表記＝**静止画だけの動画**か、本人の公式チャンネルか判別できないものばかり。 |
| 浅井健一 | ― | BLANKEY JET CITY の公式チャンネル（`@BLANKEY_JET_CITY_Official`）はあるが、**それは浅井健一 個人の棚ではない**（関所 sekisho-artist-song）。浅井健一 名義の公式動画が取れていない。 |
| Soichi Terada（寺田創一） | ― | 本人の公式チャンネルの動画が取れていない。Rush Hour / Bandcamp は音源のみ。 |
| 「真夜中のドア〜Stay With Me」の海外カバー | ― | 検索で出てくるのが**素人カバー（個人・VTuber）ばかり。**方針どおり1本も積んでいない。 |
