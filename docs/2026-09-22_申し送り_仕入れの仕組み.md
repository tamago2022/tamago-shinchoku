# 次のセッションへの申し送り（2026-09-22・仕入れ）

## 1. 門3の誤りは直した（mainに入っている）

`tools/nyuka_sekisho.py` の門3が、**ジャンルを裏返す語で棚を付けていた**。2件とも直した。

| 事故 | 何が起きていたか | 直し方 |
|---|---|---|
| フォークの人にジャズ | 公式サイトの「友川かずき（vo, g）」という演奏者表記だけで「ジャズ・ボーカル」が付いた。`vocal` はジャンルを示さない | 共起条件。ジャズの語が一緒に無ければジャズの棚に入れない |
| 自作の提供曲にカバー | 「ちあきなおみに**提供した**〜のセルフカバー」から「カバー」が付いた。**曲を書いて渡した人が、他人の曲を歌った人にされていた** | 止める語（block）を新設。「提供した／セルフカバー」があればカバーを付けない。渡した事実は「他の歌手に渡した曲」という別の棚で受ける |

- コミット `ac1f52a67`
- **再発しないように `--selftest` に見本を2件足した。**この2件が通ってしまったら関所が壊れている、と機械が落ちる。
- 実測：友川カズキ10曲にジャズの棚は1つも付かない。akiko（本物のジャズ）にはジャズが付く。特撮にはバンド。

## 2. 周辺を掘る仕組み（新規・mainに入っている）

たまごさん「自分が音楽を入れたら、その周辺や同ポジションのグループを探して仕入れ候補にしておいてほしい。（まだ棚には入れないで）」

- `tools/shuhen_horu.py` … **入荷が置かれた側から気づいて勝手に走る。**5分便に相乗り（新しい常駐は作らない）
- `tools/build_shiire_kouho_html.py` … `share/check/990-shiire-kouho.html`
- 溜め場 `status/shiire_kouho/`（★棚には書かない。`coverGuide.ts` に触らない）
- コミット `c44d9f09f`

### 掘る先は0円だけ
MusicBrainz（同定＝MBID/ISNI）／Last.fm の `+similar` **公開ページ**（APIキーを使わない）／Wikipedia。

★**Spotify の関連アーティストAPIは使わない。**公式リファレンスの
`get-an-artists-related-artists` に **Deprecated** と表示されているのを 2026-09-22 に実見した。

### 英語圏に偏る問題と、その対策
Last.fm の「似ている」は**聴き手の多い国へ流れる。**OTYKENの隣は北欧ばかりになった。
そこで**アジアのアンカー**（Hanggai／The HU／LEENALCHI／Minyo Crusaders）を必ず通す設計にした。
アジアの候補が0件で終わった回は「偏った回」として記録する。

### OTYKEN起点の実績
**候補28件（うちアジア17件）。**外したもの4群も理由つきで残してある。
→ `docs/2026-09-22_仕入れ候補_OTYKEN周辺.md`／`share/check/990-shiire-kouho.html`

**外した例がこの仕組みの効き目：**
- Alina Gingertail … ゲーム曲のカバー中心のネット発個人（素人カバーの方針に触れる）
- Korpiklaani・Alestorm ほか … Last.fmがフォークメタル一般へ流れた結果。位置がずれる
- MAMAMOO・HYUKOH ほか … LEENALCHIの隣で出たが韓国インディ一般。国楽×現代ではない

### まだ取れていない（推測で埋めていない）
- **28件すべて動画が未確認。**棚に出す前に「公式チャンネルか／静止画だけでないか」を必ず通す
- 台湾の同ポジション（原住民族の歌×現代ビート）が今回の掘り方では出てこない。別の入口が要る

## 3. フェスの名簿と「それはありませんね率」（新規）

たまごさん「誰が来ても『いや、それはないですね』っていう状態をなくしたい」

- `tools/fes_meibo.py` … 名簿と棚を突き合わせ、**穴**と**それはありませんね率**を出す（`--queue` で仕入れ待ち行列）
- `tools/build_fes_meibo_html.py` … `share/check/991-fes-meibo.html`
- 名簿 `status/fes_meibo/fujirock-2026.json`

### いまの数字（実測）
**FUJI ROCK FESTIVAL '26 ／ 数えた227組 ／ 棚にある25 ／ 穴202 ／ それはありませんね率 89.0%**

出どころは公式ラインナップページ `https://www.fujirockfestival.com/artist/index`（2026-09-22取得・全230組）。
トーク・ワークショップ・サーカスの3枠は出演者から外して率を出している（率を嘘にしないため）。

★**突き合わせは数を数えるためだけのもの。**名前が一致しただけでは同じ人と確定しない。
一致したものには `"howMatched": "名前一致（同定はまだ）"` を付けてある。
曲を入れるときは必ず入荷の関所（門2・別人を止める）と `sekisho-artist-song` を通すこと。

## 4. ★次にやること（上から）

1. **仕入れ待ち行列の上から4〜5曲ずつ積む。**`python3 tools/fes_meibo.py --queue` で出る順が、そのまま有名な順。
   1組ずつ `status/nyuka/pending/` に入荷票を置けば、Mac側の5分便が5つの門を通す。
2. **他のフェスの名簿を足す。**サマソニ／ライジングサン／ロッキン／朝霧JAM／グラストンベリー／コーチェラ／SXSW。
   `status/fes_meibo/<フェス>.json` に同じ形で置けば率は自動で出る。
3. **OTYKEN候補28件の動画を確認する**（公式チャンネルか／静止画だけでないか）。
4. プレイリスト「亮太」(31bBXeyFAGLHm1DbzVAIfK) の未入荷分。突き合わせ済みで、棚に無いのは：
   ELP「Canario」／大貫妙子「彼と彼女のソネット」／SION「夜しか泳げない」／特撮「ケテルビー」／
   Bob Dylan の『The Freewheelin'』側9曲（Masters of War, Down the Highway, Bob Dylan's Blues,
   Bob Dylan's Dream, Oxford Town, Talkin' World War III Blues, Corrina Corrina,
   Honey Just Allow Me One More Chance, I Shall Be Free）。
   ★ディランのアルバム曲は**静止画だけの動画が多い**ので、動く映像があるものだけにすること。

## 5. ★同じ穴に落ちないために（今回も落ちた）

**サンドボックスから `git commit` をしない。**`.git` 配下を作れるが消せないので `index.lock` が取り残され、
**工場全体のgitが止まる。**今回も07:23に1本取り残した（`tools/git_lock_reaper.py` が片付ける）。

正しい作法は **`git add -- <触ったファイルだけ>` で止める。**commit / pull / push はMac側の5分便がやる。
`git add -A` / `git add .` は絶対にやらない。

（出典：`docs/2026-09-18_申し送り_Coworkのgitロックとブラウザ振り分け.md`。
今回、先にこれを読まずに commit して同じ穴に落ちた。**作業の頭で docs/ の申し送りを読むこと。**）
